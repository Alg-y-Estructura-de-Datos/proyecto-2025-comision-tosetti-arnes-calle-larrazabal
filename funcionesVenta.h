#ifndef FUNCIONES_VENTA_H
#define FUNCIONES_VENTA_H

#include "Venta.h"
#include "Lista.h"

void agregarVenta(Lista<Venta>& ventas);
void eliminarVenta(Lista<Venta>& ventas);
void modificarVenta(Lista<Venta>& ventas);
